package it.polimi.ingsw.Server;

import org.junit.jupiter.api.Test;


import java.util.List;

import static org.junit.jupiter.api.Assertions.*;


class LoginHandlerTest {



/*


    @Test
    void addUsername() {

        LoginHandler handler = new LoginHandler(5);
        assertTrue(handler.getUsername().isEmpty());

        handler.addUsername("test");
        assertEquals(1, handler.getUsername().size());
        assertTrue(handler.getUsername().contains("test"));

    }



    @Test
    void checkValidUsername() {
        LoginHandler handler = new LoginHandler(5, List.of("test1", "test2"));

        assertTrue(handler.validUsername("test"));
        assertTrue(handler.validUsername("test3"));

    }

    @Test
    void checkUsernameTooLong() {
        LoginHandler handler = new LoginHandler(5);

        assertFalse(handler.validUsername("testTest"));

    }

    @Test
    void checkUsernameIllegalCharacter() {
        LoginHandler handler = new LoginHandler(5);

        assertFalse(handler.validUsername("test_"));

    }

    @Test
    void checkIllegalUsername() {
        LoginHandler handler = new LoginHandler(5, List.of("test1", "test2"));

        assertFalse(handler.validUsername("test1"));
        assertFalse(handler.validUsername("test2"));

    }

*/

}